# Build-check dependency (runs in CI regardless of your stack).
pytest>=7.0

# Web framework for this app (Secure Notes — menu #2).
flask>=3.0
# Werkzeug ships with Flask and provides password hashing + HTTP exceptions.
